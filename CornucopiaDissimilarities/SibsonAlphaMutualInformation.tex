\documentclass{article}
\usepackage{fullpage,url,amssymb]

\def\calY{\mathcal{Y}}

\begin{document}

Sibson's $\alpha$-mutual information between two random variables:

$$
\I_\alpha^S(X:Y) = \min_{Q_Y\in P(\calY)} D_\alpha(P_{XY}:P_XQ_Y).
$$

It admits a closed-form solution.

% Sibson ƒ¿-Mutual Information and its Variational Representations
\end{document}